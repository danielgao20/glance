const StartButton = () => {
  return (
    <button className="flex items-center bg-zinc-900 border-2 border-emerald-500 rounded-lg p-4 hover:bg-emerald-500">
      <span className="text-sm font-medium">Start Session</span>
    </button>
  );
};

export default StartButton;
